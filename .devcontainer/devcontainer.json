{
  "name": "Local Spark",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace/databricks-dev-container,type=bind,consistency=cached",
  "workspaceFolder": "/workspace/databricks-dev-container",
  "mounts": [
    "source=${localEnv:HOME}/.databrickscfg,target=/home/app/.databrickscfg,type=bind,readonly",
    "source=${localEnv:HOME}/.databricks,target=/home/app/.databricks,type=bind",
    "source=${localEnv:HOME}/.claude,target=/home/app/.claude,type=bind"
  ],
  "containerEnv": {
    "DATABRICKS_CONFIG_PROFILE": "sigurd.lislegaard@knowit.no",
    "DATABRICKS_WAREHOUSE_ID": "0bd7cc78c0abd6d9",
    "PYTHONPATH": "/workspace/databricks-dev-container/databricks-dev-container/src",
    "SPARK_CONF_DIR": "/workspace/databricks-dev-container/databricks-dev-container/.devcontainer/spark-conf"
  },
  "postStartCommand": "(cd databricks-dev-container && uv sync) && (grep -qF '.venv/bin/activate' ~/.bashrc || echo 'source /workspace/databricks-dev-container/databricks-dev-container/.venv/bin/activate' >> ~/.bashrc) && bash .devcontainer/scripts/check-databricks-config.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "usernamehw.errorlens",
        "anthropic.claude-code"
      ]
    }
  }
}
